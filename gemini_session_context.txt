Ringkasan Pekerjaan yang Telah Dilakukan:

*   **Perbaikan Kualitas Kode Go:**
    *   Mengidentifikasi dan memperbaiki fungsi/variabel yang tidak terpakai menggunakan `staticcheck`.
    *   Mengatasi penggunaan paket yang sudah usang (`io/ioutil`) dan memperbaiki kapitalisasi string kesalahan.
*   **Peningkatan Fitur Ekspor Excel:**
    *   Menerapkan nama file Excel yang unik dan deskriptif (menyertakan nama karyawan/perusahaan dan periode absen) untuk semua fungsi ekspor.
    *   Menambahkan warna pada header tabel di file Excel yang diekspor.
*   **Tinjauan Menu Admin Perusahaan:**
    *   Menganalisis struktur menu yang ada di panel admin perusahaan (Dashboard Overview, Employee Management, Attendance Management, Leave Request Management, Subscription Page, Payment Page, Invoice History, Settings, Location Management).
    *   Mengaktifkan filter status di frontend untuk manajemen pengajuan cuti.
*   **Implementasi Alur Permintaan Paket Kustom:**
    *   Membuat model (`CustomPackageRequest`), repositori, dan handler backend baru untuk menangani permintaan paket kustom.
    *   Mengintegrasikan formulir permintaan paket kustom di frontend (`SubscriptionPage.vue`), di mana informasi perusahaan/admin diambil secara otomatis dari sesi yang diautentikasi, dan pengguna hanya perlu memasukkan nomor telepon dan pesan.
    *   Mengaktifkan notifikasi WebSocket real-time untuk superadmin ketika ada permintaan paket kustom baru.
    *   Menambahkan UI di panel superadmin untuk melihat dan mengelola permintaan paket kustom.
*   **Implementasi Batasan Paket Langganan (Awal):**
    *   Menambahkan bidang `MaxLocations` ke model `SubscriptionPackageTable`.
    *   Memperbarui data seed di `database/seed.go` untuk paket "Standard" dan "Premium" dengan nilai `MaxLocations` yang sesuai.
    *   Menerapkan batasan `MaxLocations` di handler `CreateAttendanceLocation` untuk mencegah perusahaan melebihi batas lokasi mereka.
*   **Memulai Refaktorisasi Modular (Services Layer):**
    *   Membuat direktori `services/` dan file `services/employee_service.go` sebagai contoh lapisan layanan.
    *   Memulai proses pemindahan logika bisnis dari handler `CreateEmployee` ke `EmployeeService`.

**Langkah Selanjutnya yang Belum Selesai:**

*   **Lanjutkan Refaktorisasi `CreateEmployee`:**
    *   Selesaikan modifikasi `handlers/employee_handlers.go` untuk menggunakan `EmployeeHandler` struct dan memanggil `employeeService.CreateEmployee`.
    *   Perbarui `routes/routes.go` untuk menginisialisasi `EmployeeService` dan menyuntikkannya ke `EmployeeHandler`.
*   **Implementasi Batasan `MaxShifts`:**
    *   Tambahkan bidang `MaxShifts` ke `models/SubscriptionPackageTable`.
    *   Perbarui data seed di `database/seed.go` untuk `MaxShifts`.
    *   Terapkan batasan `MaxShifts` di handler `CreateShift`.
*   **Lanjutkan Refaktorisasi Handler Lain:**
    *   Secara bertahap pindahkan logika bisnis dari handler lain (misalnya, `UpdateEmployee`, `DeleteEmployee`, `BulkCreateEmployees`, `CreateAttendanceLocation`, `CreateShift`, dll.) ke layanan yang sesuai.
*   **Pertimbangkan Fitur Skalabel Lainnya:**
    *   Manajemen Hari Libur (Holiday Management).
    *   Manajemen Departemen/Tim (Department/Team Management) - dengan feature flagging.
    *   Laporan & Analisis Lanjutan (Advanced Reports & Analytics) - dengan feature flagging.
    *   Pengaturan Notifikasi (Notification Settings).